version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GIT_REPO: https://github.com/cloud-sue/petclinic_web.git
        GIT_BRANCH: main
    container_name: web
    ports:
      - "80:80"